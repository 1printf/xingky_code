# LabelImg Enhanced — 使用指南

> 基于 [tzutalin/labelImg](https://github.com/tzutalin/labelImg) 增强的图像标注工具

---

## 目录

1. [安装](#安装)
2. [快速上手](#快速上手)
3. [标注格式](#标注格式)
4. [界面说明](#界面说明)
5. [核心功能](#核心功能)
6. [增强功能](#增强功能)
7. [快捷键速查](#快捷键速查)
8. [配置与恢复](#配置与恢复)
9. [常见问题](#常见问题)

---

## 安装

### 环境要求

- Python 3.6+
- PyQt5
- lxml

### 方法一：pip 安装（推荐）

```bash
pip install pyqt5 lxml
git clone https://github.com/1printf/Labelimg_Enhanced.git
cd Labelimg_Enhanced
python labelImg.py
```

### 方法二：虚拟环境

```bash
python -m venv venv
source venv/bin/activate          # Linux/Mac
venv\Scripts\activate             # Windows

pip install pyqt5==5.15.2 lxml
python labelImg.py
```

### 方法三：直接打开文件夹/图片

```bash
# 打开指定图片
python labelImg.py /path/to/image.jpg

# 打开指定图片 + 预定义类别文件
python labelImg.py /path/to/image.jpg data/predefined_classes.txt

# 打开文件夹
python labelImg.py /path/to/image/directory/
```

---

## 快速上手

### 开始标注（10 秒上手）

1. **打开图片目录** → 点击工具栏 「Open Dir」或按 `Ctrl+U`
2. **选择保存目录** → 点击 「Change Save Dir」或按 `Ctrl+R`，选择标注文件存放位置
3. **创建标注框** → 按 `W` 进入画框模式，鼠标拖拽绘制矩形框
4. **选择标签** → 在弹出的对话框中输入或选择类别名称
5. **保存** → 按 `Ctrl+S` 手动保存，或开启自动保存模式
6. **切换图片** → 按 `D` 下一张 / `A` 上一张

### YOLO 格式标注流程

1. 编辑 `data/predefined_classes.txt`，写入你的类别列表（一行一个）：
   ```
   person
   car
   dog
   cat
   ```
2. 启动 labelImg 并打开图片目录
3. 点击工具栏 「YOLO」按钮切换到 YOLO 格式（或按 `Ctrl+Y`）
4. 正常标注即可，保存时会自动生成 `.txt` 文件（与图片同名）

---

## 标注格式

支持三种标注格式，通过工具栏按钮或 `Ctrl+Y` 循环切换：

| 格式 | 后缀 | 说明 |
|------|------|------|
| **PascalVOC** | `.xml` | ImageNet/PASCAL VOC 格式，最通用 |
| **YOLO** | `.txt` | YOLO 训练格式：`class_id xc yc w h`（归一化） |
| **CreateML** | `.json` | Apple CreateML 格式 |

点击工具栏上的格式按钮（显示当前格式名称）即可切换。

---

## 界面说明

```
┌─────────────────────────────────────────────────────────┐
│  菜单栏  File  Edit  View  Help                          │
├─────────────────────────────────────────────────────────┤
│  工具栏（左侧）                                          │
│  ┌──────┐                                                │
│  │ Open │  打开图片/文件夹                                │
│  │ Dir  │  打开图片目录                                   │
│  │ Save │  更改保存目录                                   │
│  │  ←   │  上一张 (A)                                    │
│  │  →   │  下一张 (D)                                    │
│  │  ✓   │  标记已验证 (Space)                            │
│  │ Paste│  粘贴+下一张 (Space)                           │
│  │ Save │  保存 (Ctrl+S)                                 │
│  │ YOLO │  格式切换 (Ctrl+Y)                             │
│  │  □   │  画框 (W)                                      │
│  │ Copy │  复制框 (Ctrl+D)                               │
│  │Combnd│  组合复制 (Ctrl+Shift+V)                       │
│  │  ×   │  删除框 (Delete)                               │
│  │  🔍  │  缩放滑块 / 亮度滑块                            │
├──────────┬──────────────────────────────────────────────┤
│ 标注列表 │                                                │
│ ┌──────┐ │                                                │
│ │person│ │              图像显示区域                       │
│ │car   │ │                                                │
│ │dog   │ │         （鼠标拖拽画框 / 右键移动）             │
│ └──────┘ │                                                │
│ □ 困难   │                                                │
│ ☑ 默认  │                                                │
│ [person▼]│                                                │
│ ┌──────┐ │                                                │
│ │person│ │                                                │
│ │car   │ │                                                │
│ │dog   │ │                                                │
│ └──────┘ │                                                │
├──────────┴──────────────────────────────────────────────┤
│  状态栏：鼠标坐标 | 当前图片信息                           │
└─────────────────────────────────────────────────────────┘
```

### 区域说明

| 区域 | 功能 |
|------|------|
| **工具栏（左）** | 文件操作、标注操作、缩放/亮度控制 |
| **标注列表面板（右）** | 当前图片所有标注框的列表，可点击选中 |
| **默认标签下拉框** | 快速设置常用标签，新建框自动使用 |
| **类别筛选下拉框** | 按类别过滤/高亮标注框 |
| **文件列表面板（右下）** | 目录中所有图片列表，双击切换 |
| **图像区域（中）** | 鼠标拖拽画框，右键移动/复制框 |
| **状态栏（底）** | 鼠标坐标实时显示 |

---

## 核心功能

### 画框与编辑

| 操作 | 方法 |
|------|------|
| **新建标注框** | 按 `W` 进入画框模式，鼠标左键拖拽 |
| **移动标注框** | 鼠标右键拖拽已有框到新位置 |
| **调整大小** | 拖拽标注框四角或四边的顶点 |
| **选中标注框** | 点击框内部区域，或从右侧列表点击 |
| **复制标注框** | 选中框后按 `Ctrl+D` |
| **删除标注框** | 选中框后按 `Delete` |
| **编辑标签名** | 双击右侧列表中的标签，或选中后按 `Ctrl+E` |

### 默认标签

勾选右侧面板的「Use default label」复选框，然后在下拉框中选择一个常用标签。之后每次新建标注框时，会**自动使用该默认标签**，无需每次手动输入。

> 下拉框中的选项来自 `data/predefined_classes.txt` 以及你的标注历史记录。

### 困难样本标记

勾选「Difficult」复选框后，新建的标注框将被标记为 difficult=1。这在 PascalVOC 格式中会被记录，训练时可根据需要排除困难样本。

### 文件导航

| 操作 | 快捷键 |
|------|--------|
| 下一张图片 | `D` |
| 上一张图片 | `A` |
| 打开图片目录 | `Ctrl+U` |
| 双击文件列表项 | 跳转到该图片 |

### 缩放与视图

| 操作 | 快捷键 |
|------|--------|
| 放大 | `Ctrl++` 或 `Ctrl+滚轮` |
| 缩小 | `Ctrl+-` 或 `Ctrl+滚轮` |
| 原始大小 | `Ctrl+=` |
| 适应窗口 | `Ctrl+F` |
| 适应宽度 | `Ctrl+Shift+F` |

---

## 增强功能

以下是本增强版本独有的功能：

### 1. 崩溃恢复（Crash Recovery）

> **防止意外退出丢失标注数据！**

- **自动备份：** 每 30 秒自动将未保存的标注保存到 `~/.labelImg_recovery/recovery.json`
- **崩溃检测：** 下次启动时自动检测是否存在未恢复的数据
- **恢复提示：** 弹出对话框，显示"文件: xxx.jpg，标注数量: 5"，选择是否恢复
- **过期清理：** 超过 7 天的恢复文件自动清除
- **正常退出清理：** 正常关闭程序时自动删除恢复文件

> **工作原理：** 程序崩溃或意外关闭后，重新打开 labelImg，会弹窗询问是否恢复上次未保存的工作。

### 2. 自动保存（Auto Save）

在 View 菜单中勾选「Auto Save mode」启用。开启后：

- 每次修改标注框（移动、调整、新建、删除）后 **400 毫秒内自动保存**
- 无需手动按 `Ctrl+S`
- 如果未设置保存目录，会在状态栏提示「Auto-save skipped: no save directory set」

> **提示：** 配合崩溃恢复使用，基本不用担心数据丢失。

### 3. 粘贴并跳转下一张（Paste & Next）

> **大幅提升连续标注效率！**

按 **空格键 (Space)** 或点击工具栏 Paste&Next 按钮：

1. 自动将**上一张图片的标注框**复制到当前图片
2. 自动**跳转到下一张图片**

适用场景：视频帧标注、连续相似图片标注（相邻帧目标位置变化不大）。

### 4. 复制上一张标注（Copy Previous）

按 `Ctrl+V`：将上一张图片的所有标注框复制到当前图片，并自动保存。

适用于两张图片中目标位置基本不变的情况。

### 5. 组合复制（Copy Combined）

按 `Ctrl+Shift+V`：同时执行两个操作——
- 复制上一张图片的标注框（Ctrl+V）
- 复制当前选中的标注框（Ctrl+D）

### 6. 亮度调节（Brightness Control）

> **解决图片过暗/过亮看不清的问题！**

工具栏底部有亮度滑块（0%-100%，默认 50%）：

| 操作 | 快捷键 |
|------|--------|
| 增亮 | `Ctrl+Shift++` |
| 变暗 | `Ctrl+Shift+-` |
| 恢复原始亮度 | `Ctrl+Shift+=` |

> 亮度调节仅影响显示，不会修改原图。

### 7. 按住 Ctrl 画正方形

- 画框时**按住 Ctrl 键**，拖拽的矩形将自动约束为正方形
- 按 `Ctrl+Shift+R` 可切换「始终画正方形」模式

### 8. 单类别模式（Single Class Mode）

在 View 菜单中勾选「Single Class Mode」。开启后：
- 所有标注框**统一使用同一个类别标签**
- 画完框不再弹出标签选择对话框
- 适合只需要标注一种目标的场景（如人脸检测、车牌识别）

### 9. 类别筛选下拉框

右侧面板顶部的下拉框列出当前图片中所有**唯一的标签类别**。选择某个类别后，该类别对应的标注框会被高亮/选中，方便快速定位。

### 10. 标注显示开关

在 View 菜单中勾选「Display Labels」或按 `Ctrl+Shift+P`，切换是否在标注框上显示标签文字。

### 11. 删除当前图片

按 `Ctrl+Shift+D` 删除当前正在查看的图片文件（从磁盘中删除），适用于清理质量差的图片。

### 12. 设置原子保存

程序设置文件（`~/.labelImgSettings.pkl`）采用**先写临时文件再原子替换**的方式保存，避免因程序崩溃导致配置文件损坏。

---

## 快捷键速查

### 文件操作

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+U` | 打开图片目录 |
| `Ctrl+O` | 打开单张图片 |
| `Ctrl+R` | 更改标注保存目录 |
| `Ctrl+S` | 保存当前标注 |
| `Ctrl+Shift+S` | 另存为 |
| `Ctrl+W` | 关闭当前文件 |
| `Ctrl+Shift+D` | **删除当前图片** |
| `Ctrl+Q` | 退出程序 |

### 标注操作

| 快捷键 | 功能 |
|--------|------|
| `W` | 创建标注框 |
| `Delete` | 删除选中的标注框 |
| `Ctrl+D` | 复制选中的标注框 |
| `Ctrl+E` | 编辑选中的标签名 |
| `Ctrl+V` | **复制上一张图片的标注框** |
| `Ctrl+Shift+V` | **组合复制（上一张+当前选中）** |
| `Space` | **粘贴上一张标注 & 跳转下一张** |

### 导航

| 快捷键 | 功能 |
|--------|------|
| `D` | 下一张图片 |
| `A` | 上一张图片 |
| `↑ ↓ ← →` | 移动选中的标注框 |

### 显示

| 快捷键 | 功能 |
|--------|------|
| `Ctrl++` | 放大 |
| `Ctrl+-` | 缩小 |
| `Ctrl+=` | 原始大小 |
| `Ctrl+F` | 适应窗口 |
| `Ctrl+Shift+F` | 适应宽度 |
| `Ctrl+Shift++` | **增加亮度** |
| `Ctrl+Shift+-` | **降低亮度** |
| `Ctrl+Shift+=` | **恢复原始亮度** |
| `Ctrl+H` | 隐藏所有标注框 |
| `Ctrl+A` | 显示所有标注框 |
| `Ctrl+Shift+L` | 显示/隐藏标注列表面板 |
| `Ctrl+Shift+P` | **显示/隐藏框上标签文字** |

### 模式切换

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Y` | 切换标注格式（PascalVOC → YOLO → CreateML） |
| `Ctrl+Shift+A` | 切换高级模式 |
| `Ctrl+Shift+R` | 切换「始终画正方形」 |
| `Ctrl+Shift+S` | 切换单类别模式 |
| `Ctrl+L` | 更改标注框线条颜色 |

---

## 配置与恢复

### 配置文件位置

- **程序设置：** `~/.labelImgSettings.pkl`
  - 保存窗口大小/位置、最近打开的文件、标注格式偏好、颜色设置等
- **崩溃恢复数据：** `~/.labelImg_recovery/recovery.json`
  - 保存最近 30 秒内的未保存标注数据

### 重置所有设置

如果启动异常或配置出错，有两种方法：

**方法一：** 菜单栏 → File → Reset All

**方法二：** 手动删除配置文件
```bash
# Linux/Mac
rm ~/.labelImgSettings.pkl
rm -rf ~/.labelImg_recovery/

# Windows
del %USERPROFILE%\.labelImgSettings.pkl
rmdir /s %USERPROFILE%\.labelImg_recovery
```

### 预定义类别文件

编辑 `data/predefined_classes.txt`，每行一个类别名：
```
person
bicycle
car
motorcycle
bus
truck
```

启动时自动加载，同时也会出现在默认标签下拉框中。

---

## 常见问题

### Q: 标注框显示为彩色而不是统一颜色？

A: 每个类别自动分配不同颜色以便区分。可在菜单 Edit → Box Line Color 中自定义。

### Q: YOLO 格式的 classes.txt 在哪里？

A: 使用 YOLO 格式保存时，`classes.txt` 会自动生成在图片所在目录（或标注保存目录），列出了当前数据集中所有出现过的类别。

### Q: 如何批量标注？

A: 目前不支持 AI 辅助自动标注。但可以用 Space 键的「粘贴 & 下一张」功能大幅加速连续帧标注。

### Q: 程序崩溃了，标注还在吗？

A: 重新启动 labelImg 会自动弹出恢复对话框。如果之前开启了自动保存或手动保存过，数据是安全的。崩溃恢复系统每 30 秒自动备份一次。

### Q: 如何修改已有标注框的标签？

A: 双击右侧标注列表中的标签项，或选中标注框后按 `Ctrl+E`。

### Q: 误删了图片怎么办？

A: 删除操作直接从磁盘删除文件，无法恢复。请谨慎使用 `Ctrl+Shift+D`。

### Q: 图片太暗看不清怎么办？

A: 使用亮度调节功能（`Ctrl+Shift++` 增亮 / `Ctrl+Shift+-` 变暗），仅影响显示，不会修改原图。

---

## 相关链接

- 原项目：[tzutalin/labelImg](https://github.com/tzutalin/labelImg)
- 增强版仓库：[1printf/Labelimg_Enhanced](https://github.com/1printf/Labelimg_Enhanced)
- Label Studio：[heartexlabs/label-studio](https://github.com/heartexlabs/label-studio)
